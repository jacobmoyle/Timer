var React = require('react');

var Timer = React.createClass({
  render: function () {
    return (
      <div>
        <h1>Timer Component Rendered</h1>
      </div>
    )
  }
});

module.exports = Timer;
